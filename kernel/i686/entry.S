.section .text.start,"ax",@progbits
.code32
.global _start
.extern kernel_main
.extern __bss_start
.extern __bss_end
_start:
    cli
    cld
    movl %ebx, %ebp              # BIOS stage2 passes OrionBootInfo in EBX
    movl $0x0009F000, %esp

    # Raw kernel images do not carry NOBITS; clear BSS explicitly.
    movl $__bss_start, %edi
    movl $__bss_end, %ecx
    subl %edi, %ecx
    xorl %eax, %eax
    rep stosb

    pushl %ebp
    call kernel_main
1:
    cli
    hlt
    jmp 1b
.section .note.GNU-stack,"",@progbits
